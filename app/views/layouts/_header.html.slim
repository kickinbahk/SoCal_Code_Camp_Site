nav.navbar.navbar-static-top role="navigation"
  .container-fluid
    h1 = link_to "SoCal Code Camp", root_path
    .navbar-header
      button.navbar-toggle.btn-lg type="button" data-toggle="collapse" data-target="#main-nav-collapse"
        .glyphicon.glyphicon-menu-hamburger
        .sr-only
          Toggle Navigation
        .icon-bar
        .icon-bar
        .icon-bar
    .collapse.navbar-collapse#main-nav-collapse
      ul.nav.navbar-nav.navbar-right
        li = link_to "About", ""
        li = link_to "Sessions", ""
        - if user_signed_in?
          li = button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn navbar-btn sign-out-btn"
        - else
          li = link_to "Sign In", new_user_session_path, method: :get



